import { SyncUrlDirective } from './sync-url.directive';

describe('SyncUrlDirective', () => {
  it('should create an instance', () => {
    const directive = new SyncUrlDirective();
    expect(directive).toBeTruthy();
  });
});
